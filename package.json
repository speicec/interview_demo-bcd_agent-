{
  "name": "blood-report-agent",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["shared", "engine", "server", "client"],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w shared && npm run build -w engine && npm run build -w server && npm run build -w client",
    "test": "npm run test -w engine"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.7.0"
  }
}
